"""Sir Convert-a-Lot package.

Purpose:
    Hold the canonical service and client implementation for conversion jobs.

Relationships:
    - `service.py` exposes the FastAPI app.
    - `interfaces.cli_app` provides the canonical Typer CLI entrypoint.
"""
