server {
    listen 8080 default_server;
    server_name _;

    add_header X-Sir-Convert-Public-Edge "reserved" always;
    add_header X-Content-Type-Options "nosniff" always;
    default_type text/plain;

    return 421 "sir-convert-a-lot-public-edge-reserved\n";
}
